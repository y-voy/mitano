<%= form_with(model: [@article, @comment], url: article_comments_path(@article) ) do |f| %>
    <%= f.text_area :content, class: "input-mysize" %>
    <%= f.submit t('submit.comment_submit'), class: "btn btn-outline-dark comment-submit float-right" %>
<% end %>
